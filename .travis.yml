language: node_js

node_js:
  - 14.16.0

cache:
  directories:
    - ./node_modules

before_install:
  - npm install -g codecov

before_script:
  - npm install --prefix ./

after_success:
  - bash <(curl -s https://codecov.io/bash)

script:
  - npm run test --prefix ./
  - codecov
